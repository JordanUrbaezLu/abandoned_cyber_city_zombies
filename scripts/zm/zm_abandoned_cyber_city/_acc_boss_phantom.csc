// =============================================================================
// _acc_boss_phantom.csc - Phantom holographic GLOW aura (client half / render half).
//
// v2 of the Phantom look (user 2026-06-18: "go"): a cyan energy glow wrapped around the
// boss so it reads as a destabilizing HOLOGRAM, not just a cloaked zombie. Same proven
// pipeline as the perk-machine glow (_acc_perk_lights.csc): server-side PlayFX does NOT
// render in this build, so the SERVER (_acc_boss_phantom.gsc) only sets an "accPhantomAura"
// clientfield on the actor, and THIS client file PlayFXOnTag's the looped glow (leak-safe:
// store the handle, StopFX before replacing).
//
// CLOAK-AWARE: the server toggles the clientfield WITH the cloak - aura ON only while the
// Phantom is materialized, OFF while cloaked - so the glow never betrays the invisible boss
// (a floating aura over an invisible body would give its position away). Materializing thus
// reads as "phases in WITH a cyan energy burst," vanishing as "the glow winks out."
//
// FX (user 2026-06-24 re-theme): Phantom = acc/light/fx_perk_glow_dark_purple (DEEP DARK PURPLE);
// Glitch Stalker = acc/light/fx_perk_glow_magenta_dim (MAGENTA at 37.5% brightness = "50% less intense"
// than the old teal). Both ALREADY packed (fx,acc/light/... in the .zone) - generated by
// tools/gen_perk_glow_fx.js. Swap level._effect below to try another colour (the whole
// acc/light/fx_perk_glow_* family is packed) without touching the server.
//
// LOCKSTEP: scope/name/version/bits/type here MUST equal _acc_boss_phantom.gsc (actor scope,
// like accEyeTint). Wired by the entry .csc #using (so this REGISTER_SYSTEM autoexec runs).
// =============================================================================

#using scripts\shared\clientfield_shared;
#using scripts\shared\system_shared;
#using scripts\shared\util_shared;

#insert scripts\shared\shared.gsh;
#insert scripts\shared\version.gsh;

// Already packed via the `fx,acc/light/fx_perk_glow_*` zone lines.
#precache( "client_fx", "acc/light/fx_perk_glow_dark_purple" );   // clientfield 1 = Subroutine Core (dark purple; was the Phantom until 2026-07-02)
#precache( "client_fx", "acc/light/fx_perk_glow_magenta_dim" );   // clientfield 2 = ex-Glitch Stalker (aura removed 2026-07-02; mapping kept for A/B re-enables)
#precache( "client_fx", "acc/light/fx_perk_glow_neon_yellow" );   // clientfield 3 = Phantom (NEON YELLOW, user 2026-07-02 toxic-skin re-theme)

#namespace acc_boss_phantom;

REGISTER_SYSTEM( "acc_phantom_aura", &__init__, undefined )

function __init__()
{
    // Shared body-aura field, COLOUR-CODED (re-themed 2026-07-02 with the toxic skins): 0 off /
    // 1 DARK PURPLE (Subroutine Core - it defaults to colour 1) / 2 dimmed MAGENTA (unused - the
    // Glitch's aura was removed; kept for A/B re-enables) / 3 NEON YELLOW (Phantom).
    level._effect[ "acc_phantom_aura_purple" ]  = "acc/light/fx_perk_glow_dark_purple";  // Core (deep dark purple)
    level._effect[ "acc_phantom_aura_magenta" ] = "acc/light/fx_perk_glow_magenta_dim";  // unused (ex-Glitch, magenta @37.5%)
    level._effect[ "acc_phantom_aura_yellow" ]  = "acc/light/fx_perk_glow_neon_yellow";  // Phantom (neon yellow, user 2026-07-02)

    // actor scope (mirror accEyeTint). 2 bits = 0..2. !CF_CALLBACK_ZERO_ON_NEW_ENT so a late joiner whose
    // snapshot already has the boss materialized still draws the aura.
    clientfield::register( "actor", "accPhantomAura", VERSION_SHIP, 2, "int", &aura_cb, !CF_HOST_ONLY, !CF_CALLBACK_ZERO_ON_NEW_ENT );
}

// self = the Phantom actor. newVal 1 = aura on (materialized), 0 = off (cloaked / dead).
// Mirrors _acc_perk_lights::glow_cb for leak + re-entry safety.
function aura_cb( localClientNum, oldVal, newVal, bNewEnt, bInitialSnap, fieldName, bWasTimeJump )
{
    if ( !isdefined( newVal ) || newVal == 0 )
    {
        if ( isdefined( self.acc_phantom_aura_fx ) )
        {
            StopFX( localClientNum, self.acc_phantom_aura_fx );
            self.acc_phantom_aura_fx = undefined;
        }
        return;
    }

    fx = undefined;
    if ( newVal == 1 )      fx = level._effect[ "acc_phantom_aura_purple" ];   // Subroutine Core (dark purple)
    else if ( newVal == 2 ) fx = level._effect[ "acc_phantom_aura_magenta" ];  // unused (ex-Glitch dimmed magenta)
    else if ( newVal == 3 ) fx = level._effect[ "acc_phantom_aura_yellow" ];   // Phantom (neon yellow, 2026-07-02)
    if ( !isdefined( fx ) ) return;

    // The model dobj must exist before PlayFXOnTag can resolve "tag_origin".
    self util::waittill_dobj( localClientNum );
    if ( !isdefined( self ) ) return;

    if ( isdefined( self.acc_phantom_aura_fx ) )
        StopFX( localClientNum, self.acc_phantom_aura_fx );

    self.acc_phantom_aura_fx = PlayFXOnTag( localClientNum, fx, self, "tag_origin" );
}
